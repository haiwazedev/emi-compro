import { PageHero } from "@/shared/components/page-hero";

export function CommitmentHero() {
  return (
    <PageHero
      breadcrumb="Home / Commitment"
      description="Certifications, corporate policies, and sustainability reports — the documents behind our promises. Uploaded and managed via CMS."
      headingId="commitment-page-heading"
      id="commitment-hero"
      title="Dokumen"
      titleAccent="Komitmen"
    />
  );
}
